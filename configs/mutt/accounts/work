set folder = ~/Mail/work

set from = '{{PROFILE_NAME}} <{{WORK_EMAIL}}>'
set realname = '{{PROFILE_NAME}}'
set smtp_url = '{{WORK_SMTP_URL}}'
set smtp_pass = '{{WORK_SMTP_PASS}}'

source ~/.mutt/mailboxes.work
source ~/.mutt/base

[general]
accounts = {{USER}}

[Account {{USER}}]
localrepository = Local
remoterepository = Remote

[Repository Local]
type = Maildir
localfolders = ~/Mail/Gmail

[Repository Remote]
type = Gmail
remoteuser = {{PROFILE_EMAIL}}
remotepass = {{IMAP_PASS}}
realdelete = no
maxconnections = 3
sslcacertfile = /etc/ssl/certs/ca-certificates.crt
folderfilter = lambda folder: folder != 'INBOX' and not folder.startswith('[Gmail]')
